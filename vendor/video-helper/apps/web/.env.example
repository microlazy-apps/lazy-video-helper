# Next.js server-side proxy target for /api/v1/* (via next.config rewrites)
API_BASE_URL=http://127.0.0.1:8000

# Recommended: keep empty so the browser always calls same-origin /api/v1/*
# and lets Next proxy to the backend (no CORS).
NEXT_PUBLIC_API_BASE_URL=

